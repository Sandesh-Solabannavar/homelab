apiVersion: apps/v1
kind: Deployment
metadata:
  name: zoom-info-scraper-ollama-desc
  namespace: zoom-info
  labels:
    app: zoom-info-scraper
spec:
  replicas: 1
  selector:
    matchLabels:
      app: zoom-info-scraper-ollama-desc
  template:
    metadata:
      labels:
        app: zoom-info-scraper-ollama-desc
    spec:
      containers:
        - name: zoom-info-scraper-ollama-desc
          image: ghcr.io/sandesh-solabannavar/zoominfo-scraper:f12b38a
          imagePullPolicy: IfNotPresent # Important for using remote images
          ports:
            - containerPort: 3000
          env:
            - name: COUNTRY
              value: US
            - name: METHOD
              value: aiDomain
            - name: ORDER
              value: desc
            - name: LIMIT
              value: "100"
            - name: CONTINUE
              value: "true"
          envFrom:
            - secretRef:
                name: zoom-info-scraper-secrets
      imagePullSecrets:
        - name: ghcr-login-secret
