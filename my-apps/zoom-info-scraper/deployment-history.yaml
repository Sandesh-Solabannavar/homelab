apiVersion: apps/v1
kind: Deployment
metadata:
  name: zoom-info-scraper-history
  namespace: zoom-info
  labels:
    app: zoom-info-scraper-history
spec:
  replicas: 1
  selector:
    matchLabels:
      app: zoom-info-scraper-history
  template:
    metadata:
      labels:
        app: zoom-info-scraper-history
    spec:
      containers:
        - name: zoom-info-scraper-history
          image: ghcr.io/sandesh-solabannavar/zoominfo-scraper:ebf6d36
          imagePullPolicy: IfNotPresent # Important for using remote images
          ports:
            - containerPort: 3000
          env:
            - name: COUNTRY
              value: US
            - name: METHOD
              value: processEmploymentHistoryCompaniesData
            - name: ORDER
              value: asc
            - name: LIMIT
              value: "100"
          envFrom:
            - secretRef:
                name: zoom-info-scraper-secrets
      imagePullSecrets:
        - name: ghcr-login-secret
