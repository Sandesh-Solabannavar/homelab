apiVersion: apps/v1
kind: Deployment
metadata:
  name: zoom-info-scraper-copy-indust-prd
  namespace: zoom-info
  labels:
    app: zoom-info-scraper
spec:
  replicas: 1
  selector:
    matchLabels:
      app: zoom-info-scraper-copy-indust-prd
  template:
    metadata:
      labels:
        app: zoom-info-scraper-copy-indust-prd
    spec:
      containers:
        - name: zoom-info-scraper-copy-indust-prd
          image: ghcr.io/sandesh-solabannavar/zoominfo-scraper:99d1380
          imagePullPolicy: IfNotPresent # Important for using remote images
          ports:
            - containerPort: 3000
          env:
            - name: METHOD
              value: copyDataIndustryProduct
            - name: LIMIT
              value: "500"
            - name: CONTINUE
              value: "true"
          envFrom:
            - secretRef:
                name: zoom-info-scraper-secrets
      imagePullSecrets:
        - name: ghcr-login-secret
