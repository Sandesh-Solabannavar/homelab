apiVersion: apps/v1
kind: Deployment
metadata:
  name: zoom-info-scraper-reverse
  namespace: zoom-info
  labels:
    app: zoom-info-scraper-reverse
spec:
  replicas: 1
  selector:
    matchLabels:
      app: zoom-info-scraper-reverse
  template:
    metadata:
      labels:
        app: zoom-info-scraper-reverse
    spec:
      containers:
        - name: zoom-info-scraper-reverse
          image: ghcr.io/sandesh-solabannavar/zoominfo-scraper:924e366
          imagePullPolicy: IfNotPresent # Important for using remote images
          ports:
            - containerPort: 3000
          env:
            - name: COUNTRY
              value: IN
          envFrom:
            - secretRef:
                name: zoom-info-scraper-secrets
      imagePullSecrets:
        - name: ghcr-login-secret
