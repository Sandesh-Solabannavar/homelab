apiVersion: apps/v1
kind: Deployment
metadata:
  name: zoom-info-scraper-germany
  namespace: zoom-info
  labels:
    app: zoom-info-scraper
spec:
  replicas: 1
  selector:
    matchLabels:
      app: zoom-info-scraper-germany
  template:
    metadata:
      labels:
        app: zoom-info-scraper-germany
    spec:
      containers:
        - name: zoom-info-scraper-germany
          image: ghcr.io/sandesh-solabannavar/zoominfo-scraper:70c1604
          imagePullPolicy: IfNotPresent # Important for using remote images
          ports:
            - containerPort: 3000
          env:
            - name: COUNTRY
              value: DE
            - name: METHOD
              value: main
          envFrom:
            - secretRef:
                name: zoom-info-scraper-secrets
      imagePullSecrets:
        - name: ghcr-login-secret
