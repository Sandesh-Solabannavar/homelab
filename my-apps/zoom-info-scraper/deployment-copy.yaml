apiVersion: apps/v1
kind: Deployment
metadata:
  name: zoom-info-scraper-copy
  namespace: zoom-info
  labels:
    app: zoom-info-scraper
spec:
  replicas: 1
  selector:
    matchLabels:
      app: zoom-info-scraper-copy
  template:
    metadata:
      labels:
        app: zoom-info-scraper-copy
    spec:
      containers:
        - name: zoom-info-scraper-copy
          image: ghcr.io/sandesh-solabannavar/zoominfo-scraper:003f004
          imagePullPolicy: IfNotPresent # Important for using remote images
          ports:
            - containerPort: 3000
          env:
            - name: COUNTRY
              value: CA
            - name: METHOD
              value: copyData
            - name: ORDER
              value: asc
            - name: LIMIT
              value: "100"
            - name: CONTINUE
              value: "true"
          envFrom:
            - secretRef:
                name: zoom-info-scraper-secrets
      imagePullSecrets:
        - name: ghcr-login-secret
