apiVersion: v1
kind: Service
metadata:
  name: homepage-dashboard
  namespace: homepage-dashboard
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: http
      protocol: TCP
      name: http
